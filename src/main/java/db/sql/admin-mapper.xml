<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

<!-- 관리자 로그인 -->
<entry key="loginAdmin">
	SELECT
		   USER_NO
		 , USER_ID
		 , USER_PWD
		 , USER_NAME
		 , STATUS
	  FROM TB_USER
	 WHERE USER_ID = ?
	   AND USER_PWD = ?
	   AND STATUS = 'A'
</entry>

<!-- 회원 공지사항 조회수 -->
<entry key="increseCountNoticeU">
	UPDATE NOTICE
		   SET COUNT = COUNT + 1
	 WHERE NOTICE_NO = ?
	   AND STATUS = 'Y'
</entry>

<!-- 회원 공지사항 목록 조회 -->
<entry key="selectNoticeListU">
	SELECT
		   NOTICE_NO
		 , NOTICE_TITLE
		 , USER_NAME 
		 , COUNT
		 , CREATE_DATE
	  FROM NOTICE N
	  JOIN TB_USER USING(USER_NO)
	 WHERE N.STATUS = 'Y'
	   AND N.TO_WHOM = 'U'
	 ORDER
	    BY NOTICE_NO DESC	 
</entry>





</properties>